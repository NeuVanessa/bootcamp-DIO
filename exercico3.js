// Exercicio Nota 2

var aluno = 'Vanessa';
media = ((5 + 3 + 3 + 2) / 4);

if (media >= 7 && media < 10) {
    console.log(`${aluno} Foi aprovado na nota de`)
}
if (media >= 10) {
    console.log(`${aluno} Mestre dos Magos miserável tu é genio sua nota: ${media}`)
} if (media < 7) {
    console.log(`${aluno} Está Reprovado Sua Nota foi:${media}`)

}

